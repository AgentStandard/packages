
## UX Reference: Moltbook API Key Rotation (2026-03-11)
- Pre-written 'copy message for your AI agent' button — eliminates operator friction on credential handoff
- 'Only shown once' warning — forces loop completion immediately
- Dark red warning box for urgency without alarm
- **Pattern to steal:** Any AgentStandard package requiring external API keys should use this exact flow: generate key ? show once ? pre-write the agent message ? one-tap copy

